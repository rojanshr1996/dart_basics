void main(List<String> args) {
  /// Optionality in dart is the ability to either have a value or not have a value
  /// in Optionals, we are playing with [Nullable] values

  const String? name = null; // Optional String. May contain null value as well
  print(name);
}
